# URL /exec del Web App de Apps Script (ver apps-script/README.md).
# No es un secreto (el navegador la expone igual), pero cada quien usa su propia hoja.
PUBLIC_SHEETS_API_URL=https://script.google.com/macros/s/XXXX/exec
